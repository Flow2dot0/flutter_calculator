class Operations{

  pourcentage({num nb}) => nb/100;
  divide({num nbLeft, num nbRight}) => nbLeft / nbRight;
  multiply({num nbLeft, num nbRight}) => nbLeft * nbRight;
  substract({num nbLeft, num nbRight}) => nbLeft - nbRight;
  addition({num nbLeft, num nbRight}) => nbLeft + nbRight;
  
}